# ==============================================================================
# .env.example
# Copy to ".env" and fill in secure values. Do NOT commit your real .env file.
# ==============================================================================

# ---------------------------
# OpenAI (used by all AI apps)
# ---------------------------
OPENAI_API_KEY=sk-your-key-here

# ------------------------------------------
# Shared AI Postgres (pgvector/pgvector:pg17)
# ------------------------------------------
POSTGRES_USER=postgres
POSTGRES_PASSWORD=secure_password

# -------------------------------------------------------
# Nextcloud (existing stack - DO NOT change variable names)
# -------------------------------------------------------
NEXTCLOUD_ADMIN_USER=admin
NEXTCLOUD_ADMIN_PASSWORD=nextcloud_password
NEXTCLOUD_DB_PASSWORD=nextcloud_db_password
REDIS_PASSWORD=redis_password

# -------------------------
# n8n (workflow automation)
# -------------------------
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=n8n_password

# -----------------------------------
# SurfSense / NextAuth configuration
# -----------------------------------
# Use a 32+ char random secret (e.g., `openssl rand -base64 32`)
NEXTAUTH_SECRET=32_char_secret
# Public URL of the SurfSense frontend (defaults to localhost 9994)
NEXTAUTH_URL=http://localhost:9994

# --------------------
# Optional OAuth (GCP)
# --------------------
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# --------------------------------
# Optional: Cloudflare (if needed)
# --------------------------------
# CLOUDFLARE_TUNNEL_TOKEN=


# Notes:
# - Keep POSTGRES_* for the AI Postgres only. Nextcloud keeps using nextcloud-db.
# - Ensure REDIS_PASSWORD and NEXTCLOUD_DB_PASSWORD match existing values.
# - After editing, save as ".env" in the repository root.